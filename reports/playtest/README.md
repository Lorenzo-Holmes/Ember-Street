# Playtest audit data

Machine-readable output generated by `npm run audit:playtest:baseline`.

The committed baseline uses 10,000 complete DAY1 -> DAY30 campaigns plus 3,000 stratified DAY29 synthetic states. CSV files are designed for spreadsheet / notebook inspection; JSON files contain the baseline configuration and automatically detected anomalies.

Human-readable interpretation lives in `docs/playtest/`.
