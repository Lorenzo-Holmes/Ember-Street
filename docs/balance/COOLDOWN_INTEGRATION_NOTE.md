# Cooldown integration checkpoint

The next validation branch should start from this audit-v2 head and add only the cross-night event recency changes. That keeps the comparison against the V2 baseline clean and lets the full Playtest Audit workflow measure the diversity delta before any resource or DAY29 balance numbers change.
